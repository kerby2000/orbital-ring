[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "orbital-ring"
version = "0.1.0"
description = "Traceable L0/L1 physics kernel for polygonal Earth orbital-ring studies"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "OR-0 / OR-1 project" }]
dependencies = [
  "matplotlib>=3.8",
  "numpy>=1.26",
  "pandas>=2.1",
  "pint>=0.23",
  "pyarrow>=15",
  "PyYAML>=6.0",
  "scipy>=1.11",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[project.scripts]
orbital-ring = "orbital_ring.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/orbital_ring"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

